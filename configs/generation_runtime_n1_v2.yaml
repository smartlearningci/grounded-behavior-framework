gemini:
  system_prompt: null
  temperature: 0.8
  max_tokens: null
  require_json: true
  output_directory: datasets/raw/n1_v2/gemini
  save_prompt_copy: true
  save_raw_metadata: true
  stop_provider_after_consecutive_failures: 5
groq:
  system_prompt: "Devolve apenas JSON válido e segue rigorosamente a instrução."
  temperature: 0.8
  max_tokens: 4000
  require_json: true
  output_directory: datasets/raw/n1_v2/groq
  save_prompt_copy: true
  save_raw_metadata: true
  stop_provider_after_consecutive_failures: 5
mistral:
  system_prompt: "Devolve apenas JSON válido. Segue rigorosamente o formato pedido."
  temperature: 0.8
  max_tokens: 4000
  require_json: true
  output_directory: datasets/raw/n1_v2/mistral
  save_prompt_copy: true
  save_raw_metadata: true
  stop_provider_after_consecutive_failures: 5
openrouter:
  system_prompt: "És um gerador de dados estruturados. Devolve apenas JSON válido."
  temperature: 0.8
  max_tokens: 4000
  require_json: true
  output_directory: datasets/raw/n1_v2/openrouter
  save_prompt_copy: true
  save_raw_metadata: true
  stop_provider_after_consecutive_failures: 5
cerebras:
  system_prompt: "És um gerador de dados estruturados. Devolve apenas JSON válido."
  temperature: 0.8
  max_tokens: 4000
  require_json: true
  output_directory: datasets/raw/n1_v2/cerebras
  save_prompt_copy: true
  save_raw_metadata: true
  stop_provider_after_consecutive_failures: 5
validation:
  enabled: true
  validate_immediately: true
  retry_invalid_json: true
  retry_wrong_example_count: true
  retry_invalid_schema: true
  retry_answer_not_in_context: false
  preserve_invalid_raw: true
